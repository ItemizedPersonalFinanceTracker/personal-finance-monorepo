# Copy to util-dockers/.env and adjust for your LAN IP.
API_HTTPS_PORT=8443
BACKEND_HOST=host.docker.internal:8000
# Include your machine LAN IP so phones match the cert SAN (still self-signed — accept once in the browser).
CERT_SAN=DNS:localhost,IP:127.0.0.1,IP:10.0.0.203
